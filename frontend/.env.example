# Copie para ".env.local" ao rodar local, ou configure na Vercel/Netlify em produção.
# URL pública do backend depois do deploy (Railway/Render), sem barra no final.
VITE_API_URL=https://SEU-BACKEND.up.railway.app
